# 项目协作规范

## 项目范围

本仓库用于长期开发由 Meta Quest 3、SSVEP 脑电、视觉识别、智能场景理解和机械臂组成的脑机接口智能机器人系统。

## 安全与资料管理

- `reference/` 中的历史项目、SDK、文档和示例默认只读；需要改造时，先复制到对应开发模块。
- 不修改原始脑电数据，不提交受试者隐私、凭据、密钥或设备序列号。
- 不运行来源不明的 exe、dll、安装器或历史项目程序。
- 不批量删除文件或目录；删除单个文件前必须确认其明确路径和用途。
- 不安装或升级 Unity、Python 包、Meta XR SDK 或系统依赖，除非用户明确授权。
- 不修改全局 Git 配置；不覆盖已有文件。

## 开发边界

- `vr_stimulus/`：未来的 VR 与 SSVEP 刺激模块；本次初始化不创建 Unity 工程。
- `vision/`：未来的视觉识别模块。
- `eeg/`：未来的脑电采集与算法模块。
- `robot_arm/`：未来的机械臂控制模块。
- `integration/`：跨模块通信与系统集成。
- `experiments/`：实验配置、方案和可复现实验记录；大型原始数据不进入 Git。

## 工作方式

- 变更前先检查现状，变更后以文件系统、测试或 Git 命令验证。
- 重要架构决定写入 `docs/decisions/`，进展写入 `docs/development_log/` 和 `docs/status/PROJECT_STATUS.md`。
- 每次提交只包含一个清晰主题，使用可读的提交信息。
- 不提前实现尚未进入当前里程碑的功能。
